# Mineplex Stats — Coming Soon

A basic landing page for an upcoming Mineplex stats tracking website.

## Local preview

You can open `index.html` directly in your browser, or use VS Code Live Server.

## Project structure

- `index.html` — page markup
- `styles.css` — styling
- `script.js` — tiny behavior (countdown + year)

## Next steps

- Connect real API/data source
- Add authentication (optional)
- Add player profile and leaderboard pages
